package com.locservice.ui.listeners;

import android.support.v4.app.FragmentActivity;

public interface MenuDrawerListener {

	void OpenMenuDrawer(FragmentActivity context);
	
	void CloseMenuDrawer(FragmentActivity context);
}
